apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hatchet.fullname" . }}-test-connection"
  labels:
    {{- include "hatchet.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hatchet.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
